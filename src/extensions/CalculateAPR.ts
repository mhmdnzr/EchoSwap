function CalculateAPR(){
    // if (rewardPerSecond && balanceOf && totalSupply) {
    //     const APR =
    //         ((rewardPerSecond * 60 * 60 * 24 * 365) / (balanceof * total_SUPPLY)) *
    //         100;
    //     if (isNaN(APR)) {
    //         return '0';
    //     } else {
    //         return APR.toFixed(2);
    //     }
    // }
    // return <CircularProgress size={20} sx={{mr:2}} />;
}
export default CalculateAPR